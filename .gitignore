# Secrets — never commit real credentials. Copy .env.example to .env locally.
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# IDE
.idea/
.claude/

# Runtime data (Excel DB, uploaded photos, generated QR codes) — the app
# stores these outside the repo by default (DATA_ROOT in .env), this local
# leftover folder from an earlier iteration isn't needed in the repo.
data/

# Unrelated local files not part of this app
python-3.13.15-amd64.exe
gemini-code-1786533928143.py
flask.log
test_download.png
